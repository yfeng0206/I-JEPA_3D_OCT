docs: separate comparison from ablation, disambiguate MIRAGE-guided

The phrase 'MIRAGE-guided masking' was ambiguous: BOTH the baseline and the
contribution use MIRAGE. mirage_envelope uses it to PLACE rectangles;
mirage_anatomy uses it to SHAPE targets to the tissue. MIRAGE guidance is not
the novelty -- shape is. Every ambiguous use is now disambiguated.

masking/ (41 KB ablations.md was a mix of unrelated topics):
  method_setup.md      what each mode is, sampler mechanics, configs
  comparison.md        arm-vs-arm results
  sampler_ablations.md masking parameter sweeps
  adapter_ablations.md adapter investigation
  engineering_notes.md implementation record
  ablations.md/findings.md reduced to compatibility stubs
All 42 figure references preserved (48 now across the split pages).

pretraining/:
  mirage_100ep.md -> envelope_100ep.md (it documented the RECTANGLE arm)
  anatomy_run_spec.md -> anatomy_ep30.md (actual status, not just a spec)
  README run table now includes the anatomy arm, records which checkpoints
  still exist (oracle's do not), and adds the planned ep25 restart

top level:
  mirage_guided_masking.md is now a disambiguation page rather than a stub
  README.md rewritten as a proper index

Also records the val-loss caveat (val uses plain MaskCollator so it is
comparable across runs but NOT against train for curriculum runs) and the
fp32/cache-key fixes.

Code: precompute_soft_guides.py now supports the encoder tap. It was hard-wired
to H0, so it could not have built a cache for the new adapter at all. The tap
is read from the checkpoint, injected via a pre-hook on proj_dec so the full
frozen decoder processes adapted features, and the cache tag now includes the
tap -- the same weights at different taps give different guides, and a stale
cache is silent.

Verified: all relative doc links resolve, no figure references lost, the
retracted 4.3x context claim survives only as its own retraction.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
Copilot-Session: 4dd84b82-282c-4eff-9f95-5f93a1ff1d8b
