fix(preflight): support the memmap guide cache form

The pre-flight globbed only *.npz, so it reported 0 volumes for a converted
.npy cache that in fact held all 6000 -- failing the launch it exists to
protect. It now counts and reads either form, names which storage it found,
and matches volumes across forms when diffing against a previous cache.

Re-run on the restart config: all checks PASS, 6000 npy (memmap) files,
8/8 volumes differ from the old cfg7 cache, 72.7%% on-anatomy, seeded.

Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
Copilot-Session: 4dd84b82-282c-4eff-9f95-5f93a1ff1d8b
