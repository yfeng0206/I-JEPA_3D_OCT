# REBUTTAL as a discovery exercise

Generated 2026-08-26. Target: `paper\genai4health2026\main_submission.tex` (NOT edited by this
report, as instructed).

## Why this document exists

GenAI4Health @ NeurIPS 2026 has **no rebuttal phase**. Nothing below can be submitted. The only
value of writing it is to find claims we can *defend with a measured artifact* but have **not put in
the paper**, and claims we **cannot** defend and must therefore concede in print. Every item
therefore ends with a **PAPER CHANGE** line naming the exact section, the kind of change (body text
/ appendix / limitation), and - for body changes - what is cut to pay for it, because the body is at
exactly 9 pages against a hard limit.

## Labels

- **MEASURED** - read directly from a named artifact in this repository.
- **INFERRED** - arithmetic or interpretation on top of measured values.
- **PENDING** - not yet observable. No value is asserted.
- **CONCEDED** - the reviewer is right and no artifact can answer them before the deadline.

## Scoreboard

- Substantive criticisms addressed: **26**
- Fully **CONCEDED** (the reviewer is right and nothing can be fixed before the deadline): **10** -
  R12, R13, R16, R17, R18, R19, R21, R22, R25, R26
- Partially conceded (defensible only after narrowing, or after a cheap fix): **6** - R4, R6, R14,
  R15, R20, R23
- Items where we hold evidence the paper does not currently present: **10** - R1 (PENDING, running),
  R2, R3, R5, R7, R8, R9, R10, R11, R24

## Body-page ledger (summary; details in each item)

| direction | item | approximate body lines |
|---|---|---:|
| CUT | Section 5.2 "Third, \ArmBest{} is the most *spatially consistent* policy..." (unbacked Gini) | -7 |
| CUT | Conclusion clause "We suggest the operative variable is the consistency of the predictive task rather than its anatomical correctness." | -2 |
| CUT | Section 5.3 sentence on the 1% fraction (`\LEOracleOne` vs `\LERandomOne` and repeat spread) | -4 |
| ADD | Section 6 replication paragraph (replaces part of "One pretraining run per policy") | +7 |
| ADD | Section 4 "Data" narrowing of the model-selection sentence | 0 (rewrite) |
| ADD | Section 5.4 one sentence on subgroup specificity at the deployed threshold | +3 |
| **net** | | **-3** |

Everything else lands in the appendix, which is unconstrained.

---

# R1. AC required item 1: independent continuation replication

**a. The criticism.**

> "Independent continuation replication for the clean claims. At least three independently
> randomized, paired post-fork continuations per policy for RANDOM, ENVELOPE, and CENTROID, all run
> from the same locked epoch-25 ancestor to the same locked endpoint, with the continuation--not the
> test subject or probe seed--as the unit of policy inference."
> - `autopilot\reports\AC_meta_review.md`, "Minimum evidence that would change my view", item 1.

> "[FATAL] [MEASURED] Every masking policy still has one pretraining continuation."
> - `autopilot\reports\P18_final_review.md`, Reviewer 1, Weakness 1.

> "[FATAL] [MEASURED] The policy effect remains \(n=1\) at the training-run level."
> - `autopilot\reports\P14_mock_review_round3.md`, Reviewer 1, Weakness 1.

**b. What we can honestly say.**

The chain that answers this is **running now**, and its design is fully specified, but **no result
exists yet**.

- **MEASURED.** Ancestor is locked by hash: `D:\jepa_phase0\checkpoints_hf\random-posfix-100ep\jepa_patch-ep025.pth.tar`,
  1,507,519,602 bytes, SHA-256 `e5ad5b0c2aadfa15449409786afbfa39d8b5405b699be8f02f2e540195e97e7b`
  (`autopilot\reports\G1_REPLICATION.md` section 2). Three independent sources agree on that value:
  the Hugging Face download, the pre-existing local mirror
  `D:\jepa_phase0\fairvision-glaucoma\checkpoint-ep25\jepa_patch-random_posfix-ep25.pth.tar`, and the
  published `SHA256SUMS`. The chain re-hashes the ancestor before every leg and aborts on mismatch.
- **MEASURED.** Six legs: {RANDOM, ENVELOPE, CENTROID} x seeds {1234, 5678}, each epoch 25 -> 50,
  configs generated by `scripts\make_replication_configs.py` which asserts that outside
  `mask.curriculum` the six configs differ only in `meta.seed` and `logging.*`.
- **MEASURED.** Endpoint epoch 50 with `optimization.epochs` held at 100, so the cosine LR, weight
  decay and EMA schedules match the originals; the stop is enforced by
  `campaign_supervisor.py --stop_after_epoch 50`.
- **MEASURED.** Chain launched 2026-08-26 16:50:50 -07:00, PID 26152, status file
  `D:\jepa_phase0\campaign\replication\chain_replication_status.json` reads
  `"stage": "train_rep_random_s1234"`. At 2026-08-26 17:19 leg 1 is inside epoch 26; no epoch line
  has been written yet.
- **MEASURED.** Leg-1 throughput over a 300 s window: 2.673 it/s (802 iterations), against 9,375
  batches per epoch. **INFERRED:** 58.5 min/epoch; 150 epochs = 146.3 h; plus six frozen probes at
  the measured prior rate of about 2.7 h each = 16.2 h; **INFERRED total about 162 h, i.e. training
  complete about 2026-09-01 and probes complete about 2026-09-02**.
- **MEASURED.** Queue is seed-major, so legs 1-3 alone (**INFERRED** about 81 h, i.e. about
  2026-08-30) give n=2 per policy for all three policies.
- **MEASURED, and a real weakness that must be printed.** `DistributedSampler` is constructed with
  its default `seed=0` and advanced by `set_epoch`, so **data visitation order is not randomised by
  `meta.seed`**. The seed randomises crop draws, target/context block draws (including the ENVELOPE
  rejection sampler and the CENTROID band sampler), dropout, and worker streams. The unit of
  inference is therefore "post-fork stochastic optimisation noise **given a fixed data order**",
  which is narrower than "independent retraining".
- **PENDING.** Every AUC, every continuation-level mean, every paired difference. Nothing about
  epochs 75 or 100 for the new legs, nothing about ANATOMY-V2 or COVER.

**The analysis plan must be fixed now, before any number is visible.** This is the only defence
available against the same adaptive-analysis criticism that already damages the paper (R15, R17).
Pre-commit to:

1. Report all nine epoch-50 continuation AUCs (3 policies x 3 continuations, counting the published
   one) in one appendix table, whatever they show. No continuation is dropped for any reason other
   than a documented crash, and a crash is reported.
2. Report per-policy mean and full range, and the two **paired-by-seed** differences
   ENVELOPE-RANDOM and CENTROID-RANDOM, one per seed.
3. At n=3 report point estimates and ranges only. No t-test, no p-value, no confidence interval on
   the continuation-level difference. Say so explicitly: three continuations bound direction, not
   magnitude.
4. State the fixed-data-order caveat in the same paragraph.

**The honest failure case, written in advance.** If the epoch-50 advantage does not reproduce - if
any paired difference changes sign, or the three-continuation range for ENVELOPE-RANDOM or
CENTROID-RANDOM spans zero - the paper says exactly this and nothing softer:

> "Across three continuations per policy from the identical epoch-25 ancestor, the epoch-50
> advantage of [policy] over unguided masking was not reproduced: the paired per-seed differences
> were [values]. The single-continuation contrasts in Table 1 therefore describe those particular
> runs and not the policies. We report them because they were measured, not because they generalise."

and the abstract's "Guidance helped" is replaced by "In the single continuations of Table 1 guidance
helped; across three continuations per policy the advantage did not reproduce." Table 1 is **not**
withdrawn in that case - it remains a correct description of the runs - but every sentence in the
abstract, contributions and conclusion that reads as a policy-level statement is downgraded to a
run-level statement.

**If the chain has not finished at freeze time**, report the completed legs only, state n per policy
explicitly, and mark the remainder PENDING. Do not extrapolate from a partial triple.

**c. PAPER CHANGE.**
- **Section 6, "One pretraining run per policy" - rewritten body text, net +7 lines**, paid for by
  cutting the Section 5.2 Gini paragraph (R12, -7) and the Conclusion mechanism clause (-2). The
  paragraph must name the design, the ancestor hash prefix, n per policy, the endpoint, the
  fixed-data-order caveat, and point to the new appendix.
- **New appendix "Continuation-level replication" - new appendix entry.** Nine-row table, per-policy
  mean and range, paired per-seed differences, the pre-committed rules 1-4 above, and the smoke-test
  evidence that the three policies were correctly instantiated (`G1_REPLICATION.md` section 7).
- **Abstract and Section 1** - one clause each, swapped not added, reporting the replication result
  or its absence.

---

# R2. Subgroup specificity and subgroup calibration at the deployed threshold - measured, computed, and not reported

This is the largest gap between what we hold and what we print.

**a. The criticism.**

> "[MAJOR] [MEASURED] The threshold analysis omits the corresponding subgroup specificity changes
> from the displayed comparison. ... Reporting sensitivity alone can make a threshold trade-off look
> like pure benefit."
> - `autopilot\reports\P18_final_review.md`, Reviewer 2, Weakness 3.

> "[MAJOR] [MEASURED] The subgroup audit remains AUC-only. Aggregate operating points and
> calibration were added, but there is still no subgroup calibration, subgroup fixed-specificity
> sensitivity, predictive-value comparison..."
> - `autopilot\reports\P14_mock_review_round3.md`, Reviewer 2, Weakness 6.

> "The operating-point appendix is useful, but at the shared threshold RANDOM and CENTROID achieve
> different test specificities, so the sensitivity difference is not measured at a common realized
> false-positive rate."
> - `autopilot\reports\AC_meta_review.md`, "Points of consensus", item 3.

**b. What we can honestly say.** All of the missing quantities are already computed and stored in
`C:\Users\Gary\Desktop\jepa\results\p16_subgroup_operating.json` (threshold `0.55517578125`,
selected on the RANDOM validation split at target specificity 0.90, transferred unchanged; the arm
key `intensity` is CENTROID). **All values below are MEASURED from that file.**

| stratum | n / n_pos | RANDOM spec | CENTROID spec | d_spec | d_sens | d_sens 95% CI |
|---|---|---|---|---|---|---|
| White | 2318 / 1080 | 0.894992 | 0.878837 | **-0.016155** | +0.034259 | [+0.019444, +0.049074] |
| Black | 431 / 268 | **0.736196** | **0.760736** | **+0.024540** | +0.003731 | [-0.026119, +0.033582] |
| Asian | 251 / 118 | 0.909774 | 0.917293 | +0.007519 | +0.008475 | [-0.033898, +0.050847] |
| overall | 3000 / 1466 | 0.879400 | 0.869622 | -0.009778 (INFERRED) | +0.026603 (INFERRED) | (Appendix I) |

The three subgroup rows are MEASURED, including the `d_spec` and `d_sens` columns, which the
artifact stores directly as `delta_random_to_intensity.race.<group>.d_specificity` and
`.d_sensitivity`. The overall row's two deltas are INFERRED arithmetic on the measured arm values.

Subgroup calibration, also stored and also unreported (**MEASURED**):

| stratum | RANDOM Brier / ECE | CENTROID Brier / ECE | d_ece |
|---|---|---|---|
| White | 0.140157 / 0.038248 | 0.132182 / 0.032057 | -0.006191 |
| Black | 0.160607 / 0.052462 | 0.155964 / 0.056921 | **+0.004459** |
| Asian | 0.122280 / 0.083180 | 0.113678 / 0.072893 | -0.010288 |
| overall | 0.141599 / 0.035518 | 0.134051 / 0.031962 | -0.003556 |

Sex strata are stored too (**MEASURED**): female sensitivity 0.685573 -> 0.711467 with specificity
0.886188 -> 0.878453; male 0.754198 -> 0.781679 with specificity 0.869634 -> 0.856916.

Three statements follow, all defensible, none currently in the paper:

1. **INFERRED from the measured values.** A single validation-selected threshold aimed at
   specificity 0.90 does not deliver it uniformly: under the null it realises 0.894992 for White and
   **0.736196** for Black patients, a 15.9-point shortfall in the same deployed screen. This is a
   property of threshold transfer on this cohort, present under **both** arms, not an effect of
   masking policy. For a health venue this is a more important observation than the AUC delta and it
   is absent from the manuscript.
2. **MEASURED.** The sensitivity gain is not a uniform trade: it is bought with specificity in the
   White stratum (-0.016155) while the Black and Asian strata gain on **both** axes
   (+0.024540 and +0.007519 specificity alongside non-significant sensitivity gains). The paper
   currently reports only the sensitivity column, which - exactly as the reviewer says - makes a
   trade look like pure benefit for the group where it is a trade, and hides a genuine two-sided
   improvement for the groups where it is not.
3. **MEASURED, and against us.** CENTROID's calibration improves overall and in the White and Asian
   strata but **worsens** in the Black stratum (ECE +0.004459). That must be printed alongside item
   2. It is the single most candour-relevant number in the artifact.

**c. PAPER CHANGE.**
- **Appendix I, Table `tab:subop` - appendix entry, extend the existing table.** Add `d_spec` and
  `d_ece` columns (already in the artifact as `delta_random_to_intensity.<attr>.<group>.d_specificity`
  and `.d_ece`) and a subgroup Brier/ECE block. Zero new computation.
- **Appendix I - new appendix paragraph.** State item 1 with both realised specificities, and say
  plainly that the shortfall exists under the null too, so it is not caused by any policy here.
- **Section 5.4 - body, +3 lines**, paid from the Section 5.3 1%-fraction cut (-4). One sentence:
  the deployed threshold does not realise its target specificity in the smallest race stratum, and
  the sensitivity gain is accompanied by a specificity loss in the largest stratum and a specificity
  gain in the other two. Cross-reference Appendix I.
- **Limitation to keep:** the comparison remains at a shared threshold, not a shared realised
  false-positive rate. Do not weaken the existing sentence that says so.

---

# R3. Table 2 is now fully artifact-backed, and the delivered-context confound is larger than printed

**a. The criticism.**

> "[PENDING] No artifact backs Table 2 as a common 600-slice, five-policy production-sampler
> measurement. ... Consequently 24 of the 25 hardcoded geometry cells are UNBACKED."
> - `autopilot\reports\HARDCODED_AUDIT.md`, "Table 2 verdict".

> "[MAJOR] The paper overstates what geometry correlation can establish."
> - `autopilot\reports\P10_mock_review_round1.md`, Reviewer 1, Weakness 8.

**b. What we can honestly say.** The audit's verdict is now **superseded by a regeneration** and we
have said nothing about it in the paper.

- **MEASURED.** `autopilot\reports\TABLE2_PROVENANCE.md`: all 25 geometry cells were regenerated
  from the production samplers via `scripts\mask_composition_probe.py` on 600 Training slices
  (24 volumes x 25 slices, stride 4, seed 42, COVER floor 0.21, batch size 1), stored at
  `results\masking\table2_geometry\mask_geometry_600slices_bs1_coverf021_seed42.json`, with two
  independent replicate draws (seeds 1234, 2026).
- **MEASURED.** Verdicts: 1 MATCHES, 21 CLOSE, 3 DIFFERS. All three DIFFERS cells are `loss slots`
  on rectangle arms, whose printed ordering (157.7 < 158.4 < 159.9 < 160.0) lies inside the
  measurement's own seed-to-seed spread (per-arm SD 0.43-1.15). Largest absolute difference over all
  25 cells is 2.21. 18 of 25 cells fall inside the three-seed range, i.e. are indistinguishable from
  a re-draw.
- **MEASURED.** Rank order by anatomy-hidden and by purity is identical to the printed table in all
  three draws, for both the four-rectangle and five-arm sets. The corrected Spearman values
  recompute to exactly **+0.80 (p=0.20)** and **+0.40 (p=0.60)** over the four rectangle arms, and
  **+0.50 / +0.20** over five.
- **MEASURED, and load-bearing.** The COVER floor is not a detail: at the script's old hard-coded
  floor 0.15, COVER hides 79.5% of anatomy and overtakes ENVELOPE, which drops the four-rectangle
  anatomy-hidden Spearman from +0.80 to +0.40. The production floor is 0.21
  (`configs\patch_cover_f021_ep25.yaml`).
- **MEASURED, and new evidence in our favour.** Table 2's `context kept` column is **per-image**
  geometry, before `MaskCollator._truncate_and_stack` truncates every context mask in a batch to the
  batch minimum (`src\masks\multiblock.py:217-228`). Measured on the same 600 slices, the context
  actually **delivered** at the production batch size of 64 is:

  | arm | printed (per image) | delivered at batch 64 |
  |---|---:|---:|
  | random | 42.1% | 24.72% |
  | centroid | 45.6% | 32.94% |
  | envelope | 40.5% | 30.66% |
  | cover | 43.5% | 30.06% |
  | anatomy-v2 | 67.9% | 63.46% |

  **INFERRED:** the anatomy-versus-rectangle context confound the paper discloses as "67.9% against
  40-46%" is, at the batch size actually trained, **63.5% against 24.7-32.9%** - roughly a doubling
  rather than a 1.6x ratio. The confound we already refuse to explain away is *larger* than the one
  we printed. This strengthens Section 5.2's "H3 is not identified" verdict rather than weakening it,
  and it is free to state.
- **MEASURED, structural.** The MIRAGE soft-guide cache
  (`C:\jepa_data\mirage_soft_guides\base512_enc_ad4f09adfa9f05f0b_m31a932eef403c3e8_npy`) contains a
  `Training` directory only, `"n_volumes": 6000`; FairVision Validation is `data_06001`-`data_07000`
  and has no guides. Three of the five production samplers therefore **cannot** be run on held-out
  slices at all. The caption's earlier "held-out" was not merely wrong, it was unachievable.

**c. PAPER CHANGE.**
- **Table 2 caption - appendix-grade change to a body caption, net-neutral rewrite.** State: (i)
  geometry is measured **per image, before batch collation**; (ii) 600 Training slices, three seeds;
  (iii) the COVER floor is 0.21.
- **New appendix "Mask-geometry provenance" - new appendix entry.** The regeneration command, the
  three-seed spread per cell, the MATCHES/CLOSE/DIFFERS table, the statement that the `loss slots`
  ordering carries no information and no claim depends on it, the COVER-floor sensitivity of the
  +0.80 coefficient, and the Training-split-only constraint on the guide cache.
- **Appendix (same entry) - the delivered-context table above**, with one sentence saying the
  identified-confound problem is larger than Table 2 suggests. Cross-reference from the existing
  Section 5.2 sentence "the anatomy arms are confounded on several axes simultaneously" - a
  reference, not new prose, so zero body lines.

---

# R4. AC required item 2: a matched anatomy intervention

**a. The criticism.**

> "A matched anatomy intervention if the title and H2 claim are retained. At least one
> segmentation-shaped arm must match ENVELOPE on delivered mask ratio, visible context, loss slots,
> target count, and collation, and must receive the same replicated continuation design through a
> common endpoint. Otherwise the acceptable claim must be narrowed to the implemented arms rather
> than 'location versus shape'."
> - `autopilot\reports\AC_meta_review.md`, minimum evidence item 2.

**b. What we can honestly say.** **CONCEDED as an experiment.** **INFERRED** cost from
`autopilot\reports\TRIAGE.md` Q26: one matched anatomy policy with three fp32-target continuations
from epoch 25 to 100 is about 341 GPU-hours on one RTX 3090; the replication chain occupies the GPU
until about 2026-09-02 (R1). It cannot be run.

What we **can** say, and do not, is exactly what "matched" would mean numerically. This converts an
abstract confession into a specification a reader can act on:

- **MEASURED.** ANATOMY-V2 sets `npred: 4` and `pred_target_k: 16` (`configs\patch_anatomy_v2.yaml`),
  giving 4 x 16 = 64 loss slots exactly; the rectangle arms do not set `K` and deliver 158.99-159.91
  slots (`results\masking\table2_geometry\...seed42.json`).
- **INFERRED.** Matching loss slots requires `pred_target_k` of about **40** (4 x 40 = 160).
- **MEASURED / INFERRED.** ANATOMY-V2 hides 21.35% of the 256-cell grid (about 55 cells) against
  40.29-46.48% (about 103-119 cells) for the rectangle arms; matching mask ratio requires roughly
  **twice** the delivered target area, which for a shape-following sampler means either more blobs
  or larger ones - a change that itself alters shape statistics.
- **MEASURED.** The two families use different collation paths (Section 5.2 already says so); a
  matched arm would additionally have to pass through the rectangle collator, which is where the
  batch-minimum truncation of R3 lives.

**c. PAPER CHANGE.**
- **Appendix (new entry, "What would identify target shape") - appendix.** The four numbers above,
  stated as the design a future run must hit: `pred_target_k` about 40, delivered mask ratio 40-46%,
  four targets, rectangle collation path, same replicated continuation design, common endpoint.
  Label the numbers INFERRED arithmetic on measured inputs.
- **Body: no addition.** Section 5.2 already contains the verdict "H3 is therefore not identified by
  this design" and the explicit refusal to claim shape is harmful. Keep both verbatim.
- **Claim narrowing (already partly done; complete it).** Anywhere the paper compares "location" to
  "shape" as concepts rather than as the implemented arms, rewrite to name the arms. The title
  change to "Does Anatomy-Guided Masking Help?" is the model; the same discipline is needed in the
  H2 paragraph of Section 5.1 and in the Conclusion.

---

# R5. The probe-count reconciliation is one sentence away and the macros already exist

**a. The criticism.**

> "the contribution list says '21 frozen probes,' the subgroup audit uses 23 valid probes, and the
> all-probe inventory contains 31 rows, without a concise reconciliation"
> - `autopilot\reports\AC_meta_review.md`, "Points of consensus", item 4.

**b. What we can honestly say.** **MEASURED.** `paper\genai4health2026\auto\auto_numbers.tex`
defines `\Nprobes` = **31**, `\NprobesSub` = **23**, `\NprobesRace` = **19**.
**MEASURED:** `autopilot\reports\P21_consistency_audit.md` records **37** frozen inventory rows
before removing six excluded/retracted rows, leaving 31 valid; the generated
`auto\table_allprobes.tex` contains 38 lines ending in a row terminator, of which one is the header.
**MEASURED:** `\Nprobes` and `\NprobesRace` are defined but **never used** anywhere in
`main_submission.tex` - the reconciliation is available at zero cost and simply was not written. The
stale hand-typed "21" has already been removed and replaced by a pointer to Appendix A, which
removes the contradiction but does not answer the reader's question.

**c. PAPER CHANGE.**
- **Appendix A opening - appendix entry, one sentence built from existing macros:** 37 inventory
  rows, of which six are excluded or retracted, leaving `\Nprobes` valid frozen probes;
  `\NprobesSub` enter the subgroup audit; `\NprobesRace` enter the race-trend subset. No new number
  is introduced and no body line is spent.

---

# R6. The direct H2 contrast is in the paper and three reviewers still could not find it

**a. The criticism.**

> "[MAJOR] [MEASURED] H2 is still argued using invalid transitive non-significance logic. ... The
> paper says an arm that does not separate from RANDOM 'cannot exceed' ENVELOPE, so no direct
> contrast is required."
> - `autopilot\reports\P18_final_review.md`, Reviewer 1, Weakness 5.

> "H2 is not directly tested; the paper incorrectly treats failure to differ from RANDOM as proof
> that an anatomy arm cannot exceed ENVELOPE."
> - `autopilot\reports\P17_blind_review.md`, "Three strongest reasons to reject", item 1.

The AC overrules them:

> "R1 is wrong for the submitted paper. Section 5.1 directly reports ANATOMY-V2 minus ENVELOPE at
> epoch 50 as \(-0.0107\), CI \([-0.0167,-0.0046]\), \(p=0.0006\)".
> - `autopilot\reports\AC_meta_review.md`, "Whether H2 is directly tested".

**b. What we can honestly say.** **MEASURED** (`auto\auto_numbers.tex`):
`\DAnatomyTwoEnvelopeEpFifty` = -0.0107, CI [-0.0167, -0.0046], p = 0.0006, q = 0.0013;
`\DAnatomyOneEnvelopeEpThirty` = +0.0044, CI [+0.0009, +0.0078], p = 0.0127. Both are printed in
Section 5.1. **INFERRED, and this is the finding:** when the same objection recurs in rounds 1, 2, 3,
4 and the blind review against text that answers it, the defect is *presentational*, not factual.
The contrast lives in a prose paragraph while every other decision-grade number lives in a table.
Reviewers read tables.

We also concede the AC's rider: **the direct contrast is causally confounded** by the budget and
collation differences of R3/R4, and must never be presented as identifying shape.

**c. PAPER CHANGE.**
- **Table 1 - body, net-neutral.** Add a two-row "direct H2 contrasts" block or a footnote line
  carrying `\DAnatomyTwoEnvelopeEpFifty` and `\DAnatomyOneEnvelopeEpThirty` with their intervals.
  Pay for it by deleting the now-redundant restatement in the Section 5.1 prose, keeping only the
  interpretation sentence ("These are different implementations at different epochs, so we do not
  read either as settling H2").
- **Table 1 caption - append one clause:** these two contrasts are confounded on delivered mask
  ratio, context, loss slots and collation (Table 2), and identify no mechanism.

---

# R7. Label-efficiency paired uncertainty is a zero-GPU analysis and the triage cost estimate is wrong

**a. The criticism.**

> "[MAJOR] [MEASURED] The label-efficiency claim lacks paired-difference uncertainty. ... Five
> repeated AUCs are reduced to per-arm means and standard deviations; repeat-level paired
> differences are not saved or reported."
> - `autopilot\reports\P18_final_review.md`, Reviewer 2, Weakness 4.

**b. What we can honestly say.**

- **MEASURED.** `results\p5_label_efficiency.json` stores only `auc_mean`, `auc_sd`, `n_repeats` per
  arm and fraction. Per-repeat AUCs are not stored, so a paired analysis cannot be recovered from
  the artifact. The reviewer is right.
- **MEASURED.** The stored values are: at 5%, CENTROID 0.833529 (SD 0.010793) vs RANDOM 0.783911
  (SD 0.013484); at 100%, 0.885644 vs 0.874805 with `n_repeats: 1`. The subsets are already paired -
  the artifact's own `method` field says "identical patient subset per fraction across arms, so arms
  are paired".
- **MEASURED, and it corrects our own triage.** `autopilot\p5_label_efficiency.py` line 1 reads
  "label-efficiency curves from cached frozen features. **Zero GPU.**" and reuses
  `runs\frozen_*\feature_cache\...`. `autopilot\reports\TRIAGE.md` Q24 costs this at "80 probe fits =
  80.0-106.7 GPU-hours", which is the cost of refitting *frozen probes*, not of refitting linear
  heads on cached features. **INFERRED: Q24 is misclassified as Class 3 and is in fact a Class 1
  zero-GPU rerun** that can be done while the replication chain occupies the GPU. It is the one
  reviewer request in the whole triage whose cost we overstated.

**c. PAPER CHANGE.**
- **Action first (not a paper change):** rerun `autopilot\p5_label_efficiency.py` with per-repeat
  AUCs persisted, then compute repeat-level paired differences per fraction and a fraction-by-arm
  contrast. Zero GPU. Until it is run, everything below stays PENDING.
- **Appendix G - appendix entry.** Add a per-repeat paired-difference column and interval per
  fraction; report the fraction-by-arm interaction or say it was not tested.
- **Section 5.3 - body, -4 lines.** Delete the 1% sentence (`\LEOracleOne` vs `\LERandomOne` plus the
  repeat-spread hedge). At n=1 subsample-repeats of 60 patients, with SDs of 0.0254-0.0368, it
  carries no information and costs four lines that R2 buys back. Keep the existing sentence "Whether
  the gap genuinely widens as labels are withdrawn we did not test".

---

# R8. The severity difference-of-differences is computable today from saved predictions

**a. The criticism.**

> "[MAJOR] [MEASURED] The severity macros do not support the claimed ordering of gains. The paired
> intervals show that mild, moderate and severe AUC each increase above zero. They do not test
> mild-minus-moderate or mild-minus-severe."
> - `autopilot\reports\P14_mock_review_round3.md`, Reviewer 1, Weakness 4.

**b. What we can honestly say.** **MEASURED** (`auto\auto_numbers.tex`, and
`auto\table_paired_subgroup.tex`): `\PDSevMildDelta` +0.01372 CI [+0.00541, +0.02192];
`\PDSevModerateDelta` +0.01016 CI [+0.00262, +0.01757]; `\PDSevSevereDelta` +0.00626
CI [+0.00104, +0.01187]. The paper already refuses the differential claim in words ("Whether mild
improves *more* we did not test"). **PENDING:** no difference-of-differences interval is stored.
**INFERRED:** the analysis is a bootstrap over the same saved per-case predictions already used for
the three contrasts above - zero GPU, same code path as the existing paired-subgroup script. The
analogous race/sex quantity is already reported (`\PDDiffRace` -0.00091 CI [-0.02379, +0.02139];
`\PDDiffSex` +0.00146 CI [-0.00853, +0.01156]), so severity is the only stratification where we
report the contrasts against zero but not the contrast between them - an inconsistency of our own
making.

**c. PAPER CHANGE.**
- **Appendix C or the subgroup appendix - appendix entry.** Add mild-minus-moderate and
  mild-minus-severe paired bootstrap differences with intervals, computed from the stored
  predictions. If they are not computed before freeze, leave the existing "we did not test" sentence
  exactly as it is and add nothing.
- **No body change.** The abstract's hedge ("largest for mild ... though the gains are not separated
  from one another") is already correct and must not be strengthened.

---

# R9. The multiplicity family is exactly nine and the artifact now says so

**a. The criticism.**

> "[MAJOR] The multiplicity accounting in the PDF is factually inconsistent with the artifact.
> Table 2 says \(q\) is BH 'over these nine' contrasts, but `p1c_stats.py` and `p1c_stats.json` apply
> BH over **13** same-epoch/same-precision contrasts".
> - `autopilot\reports\P10_mock_review_round1.md`, Reviewer 1, Weakness 5.

**b. What we can honestly say. This is resolved and we can prove it.** **MEASURED.**
`D:\jepa_phase0\autopilot_out\p1_stats\p1c_stats.json` now contains
`multiplicity.confirmatory_family_size = 9`, `exploratory_family_size = 25`, with the stored note
"BH applied separately within each family; the confirmatory family is exactly the nine fp16
contrasts among random/oracle/envelope at epochs 50/75/100 that the manuscript reports". The paper's
"family of nine" is therefore correct as printed.

**c. PAPER CHANGE.**
- **Appendix L (full paired-contrast table) - appendix, one clause.** State that BH is applied
  within a pre-declared confirmatory family of nine and that a separate exploratory family of 25 is
  corrected independently. This is a defensible statement we currently omit, and it inoculates
  against a criticism that has already been raised once.

---

# R10. The fork point and the replication configs are auditable, and we say almost nothing about it

**a. The criticism.** Implicit across the panel: reproducibility claims are made but the artifacts
are described loosely. Nearest explicit form:

> "Which exact PDF is the submission, what is its hash, and what automated gate checks it?"
> - `autopilot\reports\TRIAGE.md`, Q04.

**b. What we can honestly say.** **MEASURED:** the epoch-25 ancestor's SHA-256 is
`e5ad5b0c...e97e7b`, agreed by three independent sources (R1). **MEASURED:**
`scripts\make_replication_configs.py` generates the six replication configs and asserts that they
are identical outside `mask.curriculum` except for `meta.seed` and `logging.*`, printing
`INVARIANTS OK`. **MEASURED:** `autopilot\run_guarded_probe.py` hashes the encoder before and after
each probe and invalidates the run if the hash moves. **MEASURED:** `autopilot\p13_build_zip.py`
checks standalone compilation, the nine-page limit, undefined references and citations, and hashes
staged files. **MEASURED on 2026-08-26:** `main_submission.pdf` SHA-256
`5DF14D65C77735DEA3036CBC7FC3C4E484CA6E419CF8513A3C6EC706B56F69D7` - which must be recomputed after
any edit and must not be presented as permanent.

**c. PAPER CHANGE.**
- **Appendix (reproducibility) - appendix entry.** Ancestor hash prefix, the generated-config
  invariant, and the encoder-hash guard on every probe. These are cheap, verifiable, and the kind of
  claim a sceptical reviewer can check without weights.
- **No body change.** The existing anonymised release sentence in Section 5.5 stays as is.

---

# R11. The Spearman correction, and the fact that the coefficient depends on the COVER floor

**a. The criticism.**

> "Section 5.2's correlations contradict the table caption directly above them."
> - `files\R4_narrative_review.md`, finding N2.

**b. What we can honestly say.** **MEASURED** (`autopilot\reports\SPEARMAN_CORRECTION.md`): the
submitted text said the anatomy-hidden/AUC Spearman was "exactly 0.00" and purity "trends negatively
(-0.40)"; recomputation on Table 2's own values at matched epoch 50 gives **+0.80 (p=0.20)** and
**+0.40 (p=0.60)** over the four rectangle arms. Both are positive; the sentence "neither shows the
positive relationship the anatomy-guidance hypothesis predicts" was exactly backwards. Corrected at
all four propagation sites (abstract, contribution bullet, Section 5.2, conclusion), and the claim
now rests on the direct comparison instead: anatomy-v2 places 97.3% of masked cells on tissue and
does not separate from the null, CENTROID places 41.1% and reaches the highest epoch-100 AUC.
**MEASURED** (`TABLE2_PROVENANCE.md` section 7): regeneration reproduces +0.80 and +0.40 exactly, in
all three seed draws.

**The unstated part.** **MEASURED:** at COVER floor 0.15 the four-rectangle anatomy-hidden Spearman
falls to +0.40. The coefficient is a function of a configuration constant. We print the coefficient
and not the sensitivity.

**The standing lesson, which belongs in the paper's own methodology.** **MEASURED:** the wrong
numbers were hand-typed in the TeX, so every gate - which verifies generated macros - was blind to
them. `autopilot\reports\HARDCODED_AUDIT.md` finds **310** hardcoded numeric occurrences, of which
**234 CONFIRMED, 75 UNBACKED, 1 WRONG** (the 7.17M fine-tuned probe parameter count at line 1060;
the stored 64-slice configuration gives 7,140,096 = 7.14M; now fixed).

**c. PAPER CHANGE.**
- **Appendix (mask-geometry provenance, same entry as R3) - appendix.** State the COVER-floor
  sensitivity of the +0.80 coefficient explicitly.
- **Section 5.2 - no change.** The corrected sentence already declines to draw inference at n=4-5.
  Do not add.
- **Appendix (reproducibility) - appendix, one sentence.** Report the hardcoded-number audit counts
  (310 / 234 / 75 / 1) and the corrective rule adopted: a numeric claim that is not a macro is
  treated as unverified until recomputed. This is the paper's strongest credibility asset and it is
  currently invisible.

---

# R12. CONCEDED: the "spatial consistency" mechanism rests on an unbacked Gini and the nearest artifact points the other way

**a. The criticism.**

> "[MINOR] The manuscript uses strong mechanism language after correctly admitting mechanism is
> unidentified."
> - `autopilot\reports\P10_mock_review_round1.md`, Reviewer 3, Weakness 8.

**b. What we can honestly say. CONCEDED, and worse than the reviewer knew.** The paper says
(Section 5.2, third observation):

> "Its per-cell hit map is the most concentrated of any arm (Gini \(0.400\) versus \(0.316\)-\(0.338\)
> for the other rectangle arms)"

- **MEASURED.** `HARDCODED_AUDIT.md` line 519: UNBACKED. No producing artifact found.
- **MEASURED.** The only stored Gini in the repository is
  `results\masking\coverage\coverage.json`: `random_default` **0.4111302367507499**,
  `random_matched` 0.28943627281526546, `envelope_default` **0.4037652861537525**, `anatomy`
  0.3316935146316607. **There is no CENTROID entry at all**, and the producing script
  `scripts\coverage_probe.py` does not include a CENTROID arm.
- **INFERRED.** If that artifact were the source, RANDOM (0.411) and ENVELOPE (0.404) would both be
  *more* concentrated than the 0.400 attributed to CENTROID, which reverses the sentence's claim.
  We cannot assert the reversal - the artifact is a different diagnostic on a different slice set -
  but we equally cannot assert the printed claim.

This matters beyond one sentence: it is the sole quantitative support for the Conclusion's
"We suggest the operative variable is the consistency of the predictive task rather than its
anatomical correctness", and for the same mechanism sentence in Section 6.

**c. PAPER CHANGE.**
- **Section 5.2 - body, CUT the whole "Third, ..." paragraph (-7 lines).** This is the payment for
  R1's replication paragraph.
- **Conclusion - body, CUT the mechanism clause (-2 lines).** Replace with nothing; the preceding
  sentence already ends the paper on a defensible statement.
- **Section 6 - keep** the existing "nor is \ArmBest{}'s mechanism [identified]" sentence and its
  two candidate explanations, which are labelled as unresolved and cost nothing.
- **Optional appendix, only if measured before freeze:** extend `scripts\coverage_probe.py` to all
  five production arms (CPU-only, no GPU, does not contend with the replication chain) and report
  per-cell hit-map Gini for all of them. If it is run and CENTROID is not the most concentrated arm,
  print that. If it is not run, the claim stays deleted.

---

# R13. CONCEDED: the probe-seed standard deviations cannot be reproduced

**a. The criticism.** Raised by the AC as a point in our favour, which makes it worse:

> "with five probe seeds on two fixed encoders the probe-seed standard deviation is \(0.0003\) and
> \(0.0018\)" is cited in `AC_meta_review.md`, "Points of consensus", item 1, as the paper's one
> quantitative bound on probe noise.

**b. What we can honestly say. CONCEDED.**
- **MEASURED.** `HARDCODED_AUDIT.md` line 656: UNBACKED.
- **MEASURED.** `paper\genai4health2026\research\verify_sections_4_6.md`, findings C25 and C27:
  "Exhaustive `D:\jepa_phase0\runs\**\results.json` scan for the two ep30 checkpoints. Only
  `frozen_meanpool_anatomy_ep30\results.json` and `frozen_meanpool_envelope_ep30\results.json` were
  found. ... The other four probe-seed outputs needed to regenerate means, SDs, Welch p, and d were
  not found." Verdict **UNVERIFIABLE**. The surviving seed-42 values are 0.85827430 and 0.85391695.
- **INFERRED.** Regenerating the eight missing probe fits costs about 8-11 GPU-hours at the measured
  1.0-1.33 h per frozen probe, and the GPU is committed to the replication chain until about
  2026-09-02 (R1). It is the lower-value use of any free slot.

So the paper's only numerical bound on probe-seed noise - and therefore the sentence "probe noise is
three to seven times below the effects in Table 1" - is not currently reproducible from stored
artifacts.

**c. PAPER CHANGE.**
- **Section 6, "One pretraining run per policy" - body, net-neutral rewrite.** Either (i) regenerate
  the eight probe fits and keep the sentence, or (ii) replace it with the stored, verifiable fact:
  two encoders probed at seed 42 give 0.858274 and 0.853917, a paired bootstrap difference of
  +0.00436, CI [+0.00097, +0.00776] - and state that a five-seed probe-noise estimate was computed
  historically but its per-seed artifacts are not retained. Option (ii) is shorter and costs no GPU.
- **Do not** keep the "three to seven times" comparison without the underlying SDs.

---

# R14. CONCEDED (partly): the COVER postmortem's headline numbers have no persisted artifact

**a. The criticism.**

> "[MAJOR] [MEASURED] The COVER failure was not isolated to an obscure metric. Only 32.5% of images
> retained all four targets..."
> - `autopilot\reports\P14_mock_review_round3.md`, Reviewer 1, Weakness 7 (accepting our own numbers).

The AC calls Appendix E "the clearest example" of valuable disclosure. That makes its provenance
load-bearing.

**b. What we can honestly say.**
- **MEASURED, and against us.** `HARDCODED_AUDIT.md` lines 915-916 and 919: the pre/post-truncation
  coverage figures (78.62% / 73.88%) and the retained-target statistics (32.47% of images keep all
  four targets; mean 2.51 of 4) are **UNBACKED**. They exist only as narrative in
  `autopilot\COVER_AUDIT.md`, which states they came from a "fresh CPU audit, 194 accepted slices"
  but names no persisted output file.
- **MEASURED, and in our favour.** The delivered-coverage comparison that the *body* relies on is
  fully backed: `D:\jepa_phase0\reports\arm_stats_sweep\cover_floor_sweep.json` gives COVER
  `pct_anat_hid` = 73.0945185679789 and ENVELOPE 77.58224558657956, matching the printed 73.1% and
  77.6%. The claim "COVER hides less anatomy than ENVELOPE" therefore survives even if the
  postmortem's internal numbers do not.

**c. PAPER CHANGE.**
- **Appendix E - appendix.** Re-run the COVER audit so it writes a JSON, and cite it; the audit is
  CPU-only and can run beside the training chain. If it is not re-run before freeze, mark the four
  affected numbers as "measured in a one-off audit on 194 slices; raw output not retained" - which
  is embarrassing but true, and is exactly the standard the rest of the paper is held to.
- **Body: no change.** The body only uses the 73.1% / 77.6% pair, which is backed.

---

# R15. Partly conceded: "no test volume is seen during model selection" is too broad

**a. The criticism.**

> "'no test volume is seen during model selection' conflicts with outcome-dependent stopping and
> acknowledged adaptive test inspection".
> - `autopilot\reports\AC_meta_review.md`, "Clarity: R3 versus R1/R2 and R4".

**b. What we can honestly say.** **MEASURED.** Section 4 "Data" currently reads "No test volume is
seen during pretraining, probe fitting, or model selection: the probe head is selected on a separate
validation split." **MEASURED.** Section 6 says the opposite about the research programme: "policies,
checkpoints and analyses were chosen after repeated inspection of that split". Both cannot stand as
written. The first is true of the *probe head*; it is false of the *study*.

**c. PAPER CHANGE.**
- **Section 4, "Data" - body, net-neutral rewrite.** "No test volume is used to fit or select the
  probe head; the head is selected on a separate validation split. The research programme's choices
  of policy, checkpoint, analysis and stopping horizon were made after repeated inspection of this
  same test split (Section 6)." Same length, no contradiction, and it forward-references the
  limitation instead of contradicting it.

---

# R16. CONCEDED: AC required item 3, a genuinely untouched evaluation set

**a. The criticism.**

> "A genuinely untouched evaluation set for the final locked comparison. This is necessary because
> the submitted test split influenced policies, checkpoints, analyses, and stopping. A new cohort
> would also begin to address R2's external-validity concern."
> - `autopilot\reports\AC_meta_review.md`, minimum evidence item 3.

**b. What we can honestly say. CONCEDED, without qualification.**
- **MEASURED.** The only labelled OCT cohort on either drive is FairVision glaucoma:
  `D:\jepa_phase0\fairvision-glaucoma\data` contains exactly `Training`, `Validation`, `Test`, with
  one metadata file `data_summary_glaucoma.csv`. `D:\jepa_phase0\datasets` holds `coco2017`,
  `imagenet50`, `smoke`, `source`; `D:\jepa_phase0\mirage-datasets` holds MIRAGE segmentation merges.
  There is no second glaucoma cohort, no second scanner, no second disease.
- **MEASURED.** The Validation split is not untouched either: it is where every probe head is
  selected.
- **PENDING / no evidence.** `TRIAGE.md` Q28 records no prepared untouched cohort and no measured
  acquisition or harmonisation time.

The honest answer is: the reviewer is right and we cannot fix this before the deadline. The only
partial mitigation available is procedural, not evidential - pre-committing the replication analysis
before its numbers are visible (R1) - and it must not be described as an untouched evaluation set,
because it is not.

**c. PAPER CHANGE.**
- **Section 6, "Scope, coverage and an incomplete arm" - limitation, net-neutral.** Keep the existing
  adaptive-reuse sentence and add, inside it, that no untouched cohort was available and that the
  study therefore reports no confirmatory evaluation. Do not soften.
- **Appendix (limitations) - appendix.** State what an untouched evaluation would require: a
  labelled cohort from a different scanner or population, thresholds selected on its own validation
  split, one evaluation, no iteration. Naming the missing design is worth more than apologising for
  it.

---

# R17. CONCEDED: we cannot say how many times the test split was inspected

**a. The criticism.**

> "Exactly how many times was FairVision test AUC inspected while selecting policies, \(f=0.21\),
> checkpoints, framing, and subgroup analyses?"
> - `autopilot\reports\TRIAGE.md`, Q32 (from P10 Reviewer 1, question 4).

**b. What we can honestly say. CONCEDED.** **PENDING:** no inspection ledger exists anywhere in the
repository. The count is unrecoverable. The consequence - that every interval in the paper is
descriptive rather than confirmatory - is already stated in Section 6 and must stay.

**c. PAPER CHANGE.** **Section 6 - limitation, no length change.** Add four words to the existing
sentence: the number of inspections was not logged and cannot be reconstructed. Nothing else can be
done.

---

# R18. CONCEDED: no pre-specification, and no equivalence margin

**a. The criticism.**

> "Where were comparisons/checkpoints/subgroups pre-specified, and were demographic/severity
> analyses planned before outcomes?" - `TRIAGE.md` Q12.
> "What prespecified equivalence margin would establish that anatomy 'does not help'?" - `TRIAGE.md`
> Q33 (P10 Reviewer 1, question 5).

**b. What we can honestly say. CONCEDED on both.** **PENDING:** no timestamped protocol and no
prespecified equivalence or non-inferiority margin exists. **MEASURED:** the word "pre-specified"
no longer appears in `main_submission.tex` (checked), so the first half is already repaired by
deletion. The second half remains: failure to reject a difference is not equivalence, and inventing
a margin now would be post-hoc.

**c. PAPER CHANGE.**
- **Section 5.1 and Section 6 - limitation, net-neutral.** Ensure every "indistinguishable" is
  accompanied by the observed delta and interval rather than standing alone. Section 5.1 already
  does this for `\DAnatomyTwoRandomEpFifty` (+0.0013, CI [-0.0055, +0.0082], p = 0.7153); apply the
  same rule everywhere.
- **Do not** introduce a retrospective equivalence margin.

---

# R19. CONCEDED: ANATOMY-V2 was stopped after seeing an unfavourable test result

**a. The criticism.**

> "[MAJOR] [MEASURED] ANATOMY-V2 was stopped after inspecting an unfavorable test result. ... This
> is not a defensible scientific stopping rule."
> - `autopilot\reports\P18_final_review.md`, Reviewer 1, Weakness 2.

**b. What we can honestly say. CONCEDED.** **MEASURED:** the arm moved from `\DAnatomyTwoRandomEpFifty`
+0.0013 at epoch 50 to `\DAnatomyTwoRandomEpSeventyFive` -0.0111 (CI [-0.0181, -0.0042], p = 0.0017)
at epoch 75 and was then stopped. The current text already says the horizon was selective, that a
trajectory can recover, and that epoch 100 is "unmeasured, not known to be worse" - which the AC
accepted as internally coherent.

**INFERRED, on feasibility:** completing the arm costs about 38.5-38.9 GPU-hours (`TRIAGE.md` Q27:
25 epochs at the measured 90.1 min/epoch for fp32 targets, plus one probe). The GPU is committed
until about 2026-09-02 (R1); against a 2026-09-05 deadline that leaves roughly 60 h, so Q27 would
fit only if the chain does not slip and nothing else is queued. **Recommendation: do not commit.** A
half-finished second arm at freeze time is worse than a disclosed unmeasured endpoint.

**c. PAPER CHANGE.** **Section 4 "Pretraining" and Section 6 - limitation, no change.** The
disclosure is already correct and calibrated. Do not add a prediction about epoch 100 in either
direction.

---

# R20. Partly conceded: ENVELOPE's containment language

**a. The criticism.**

> "ENVELOPE is oversold as being 'within' or 'restricted to' retinal tissue ... a mask with 43.5%
> purity is not literally restricted to retinal tissue."
> - `autopilot\reports\P17_blind_review.md`, section 2.

**b. What we can honestly say.** **MEASURED:** ENVELOPE's purity is 43.30% in the regenerated
600-slice artifact (43.5% printed), i.e. more than half of its masked cells are background. The
paper already concedes this once in Section 5.1 ("Aiming is not containment: the rectangles are
large, so only 43.5% of ENVELOPE's masked cells land on tissue"). The defect is that the abstract,
the policy list ("random-within-retina"), the Conclusion and two figure captions still use
containment verbs.

**c. PAPER CHANGE.**
- **Section 3.2, abstract, Conclusion, the Figure 1 caption and the mask-statistics figure caption -
  body, net-neutral.** Replace "restricting targets to retinal tissue" and "random-within-retina"
  with "rectangle centres rejection-sampled onto the predicted retinal envelope" (method) and
  "retina-biased placement" (short form). Every instance is a swap, not an addition.

---

# R21. CONCEDED: COVER names an intervention that was not delivered

**a. The criticism.**

> "As a name for the experimental treatment actually delivered, it is misleading: the arm did not
> realize aggressive coverage."
> - `autopilot\reports\P17_blind_review.md`, section 2.

**b. What we can honestly say. CONCEDED.** **MEASURED:** delivered COVER hides 73.09% of anatomy
against ENVELOPE's 77.58% (`cover_floor_sweep.json`), the opposite of its design intent. The paper
already says the arm "never realised the coverage it was configured for" and that its trajectory "is
not evidence about aggressive coverage".

**c. PAPER CHANGE.** **Section 3.2 and Section 5.1 - body, net-neutral.** Use one fixed phrase
throughout - "a defective coverage-seeking arm, whose targets were truncated after placement" - and
never let the bare name COVER stand in a sentence that draws an inference about coverage. No length
change.

---

# R22. CONCEDED: external validity

**a. The criticism.**

> "[MAJOR] [MEASURED] External validity remains extremely narrow. There is one dataset, one glaucoma
> label, one segmentation model, one test split, and no independent scanner, population, disease, or
> dense retinal task."
> - `autopilot\reports\P18_final_review.md`, Reviewer 2, Weakness 5.

**b. What we can honestly say. CONCEDED** - see R16 for the artifact evidence that no second cohort
exists locally. The existing Section 6 sentence already states the scope limit and explicitly
declines to generalise to tasks where anatomy is the output.

**c. PAPER CHANGE.** **Section 6 - limitation, no change.** Do not add hedging; it is already there.
The correct response is R16's appendix statement of what an external evaluation would require.

---

# R23. Partly conceded: novelty

**a. The criticism.**

> "[MAJOR] The high-level phenomenon is not novel. The paper's own related work already documents
> both halves of the story".
> - `autopilot\reports\P10_mock_review_round1.md`, Reviewer 3, Weakness 2.

**b. What we can honestly say.** The AC settles this at 3/6: the direction is known, the controlled
OCT/I-JEPA measurement against a *trained medical segmenter*, the segmentation-free comparator, and
the COVER postmortem are moderate workshop originality. **MEASURED:** Section 2 already contains the
paragraph "Evidence that informed masking is not uniformly better" and Appendix K tabulates the
published ablations, so the paper does not claim the direction is new; Appendix K's closing sentence
now reads "What we add is the controlled measurement, not the sign of the result."

**c. PAPER CHANGE.** **Contributions list - body, net-neutral.** Make the first contribution the
controlled rectangle-family comparison plus the masking-pipeline audit, with CENTROID as the
inexpensive comparator and the subgroup work as ancillary, per `TRIAGE.md` Q16. This is a reordering,
not an addition.

---

# R24. The appendix contains a second project; the body is at its limit

**a. The criticism.**

> "Pages 18-22 more generally read as a second project's appendix stapled on."
> - `files\R4_narrative_review.md`, section 1.

> "several appendices develop secondary projects rather than supporting the decision-critical
> claims. The five-page occlusion/laterality sequence is the clearest example".
> - `autopilot\reports\AC_meta_review.md`, "Points of consensus", item 5.

**b. What we can honestly say.** **MEASURED:** `autopilot\reports\R4_FIXES.md` records that all 21
previously orphaned labels are now cited (0 dangling), that Appendix H's heading "Why this is in a
masking paper" became the declarative "Diffuse attribution supports the main claim", and that
Section 5.2 now cites it. **MEASURED, and against us:** `HARDCODED_AUDIT.md` lists the entire
Appendix H numeric set (0.94, 63/137/95, -0.22/-0.07/-0.14, 0.971/0.988, -0.124/-0.478, 0.25,
84%/91%/95%, 0.35/0.48) as UNBACKED, because "the NPZ inputs live on an external blob/local archive
that is absent from this checkout". So the least decision-critical appendix is also the least
verifiable one.

**INFERRED:** cutting appendix pages does not buy body pages, so this is not a page-budget action -
it is a credibility action. An appendix whose numbers cannot be recomputed weakens a paper whose
whole argument is that its numbers can be.

**c. PAPER CHANGE.**
- **Appendix H - appendix.** Retain only the diffuse-attribution result that Section 5.2 cites.
  Move the OD/OS laterality forensics and the confidence-invariance material out, or mark every
  number in them as "computed from archived NPZ inputs not included in the release".
- **Appendix H - required wording fix regardless.** The laterality claim must read "an
  orientation/storage mixture consistent with OD/OS" rather than a direct signature: **PENDING**,
  no eye-laterality label or validation table exists (`TRIAGE.md` Q36).

---

# R25. CONCEDED: the background-mechanism question is open, and the two settling controls were not run

**a. The criticism.**

> "Is the high background-only AUC really 'attention leakage' rather than background signal?"
> - `autopilot\reports\TRIAGE.md`, Q37 (from R4 finding N9).

**b. What we can honestly say. CONCEDED.** **MEASURED:** `R4_FIXES.md` records that the causal
"attention leakage" diagnosis was already replaced with the architectural statement plus an explicit
admission that the amount of mixing was not measured. **MEASURED:**
`paper\genai4health2026\research\mechanism.md` reports background-position AUCs up to 0.870075 while
stating that global token mixing means this does not establish a black-pixel content signal.
**PENDING:** neither settling control has been run - the eroded-background regional probe
(**INFERRED** 0.67 GPU-hours for four arms) and the background-content shuffle with positions and
token count fixed (**PENDING**, never benchmarked).

**c. PAPER CHANGE.** **Appendix F - appendix, one sentence.** Name the two controls explicitly and
mark both as not run. R4's fix removed the vague "two ablations would settle it"; naming them is
strictly better than either the vague version or silence, and costs no body space.

---

# R26. CONCEDED: an artifact the paper's own source list promises does not exist

**a. The criticism.**

> "[MINOR] The promised independent DeLong validation artifact is absent. `SOURCES.md` lists
> `delong_validation.json`, but that file is not present under
> `D:\jepa_phase0\autopilot_out\p1_stats`; only the validation script exists."
> - `autopilot\reports\P10_mock_review_round1.md`, Reviewer 1, Weakness 9.

**b. What we can honestly say. CONCEDED, still true today.** **MEASURED 2026-08-26:**
`Test-Path D:\jepa_phase0\autopilot_out\p1_stats\delong_validation.json` returns **False**. The
directory contains 13 JSON artifacts and that is not one of them.

**c. PAPER CHANGE.** **Release record / `SOURCES.md` - not a paper change but a freeze-blocker.**
Either run the validation script and persist its output, or delete the promise. A source list that
names a file which does not exist is the cheapest possible way to lose a reviewer's trust, and this
one has already been caught once.

---

# Verified clean - previously raised, now confirmed absent from the source

Checked directly in `main_submission.tex` on 2026-08-26; no action needed, listed so they are not
re-raised:

- "pre-specified" / "prespecified": **absent** (MEASURED).
- "Rows appear as the re-probes complete": **absent** (MEASURED).
- "21 frozen probes": **absent**, replaced by a pointer to Appendix A (MEASURED).
- "600 held-out slices": **absent**; the caption now says 600 training slices (MEASURED), which is
  the only supportable phrasing given the Training-only guide cache (R3).
- BH family size: paper says nine, artifact says nine (MEASURED, R9).
- Direct H2 contrast: present in Section 5.1 (MEASURED, R6) - the problem is placement, not absence.

---

# What this exercise found, in one paragraph

Three things are defensible today and unsaid: **subgroup specificity and calibration at the deployed
threshold** (R2 - including that a validation-selected threshold realises 0.736 specificity for
Black patients against 0.895 for White, under both arms), **the regenerated Table 2 with its
three-seed noise bound and the delivered-context measurement** (R3 - which shows the confound we
already refuse to explain away is about twice as large as printed), and **the replication design
itself** (R1 - a locked ancestor hash, generated configs with an asserted invariant, and a
pre-committed analysis plan that includes the case where the effect vanishes). Two things are not
defensible and must be removed or restated: the **Gini-based "spatial consistency" mechanism**
(R12), whose only nearby artifact points the other way, and the **five-seed probe-noise bound**
(R13), whose per-seed outputs no longer exist. And the AC's third requirement - an untouched
evaluation cohort - **cannot be met at all** (R16): there is exactly one labelled cohort on this
machine, and its validation split is already spent on head selection.
