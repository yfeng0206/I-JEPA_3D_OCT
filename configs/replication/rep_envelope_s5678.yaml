# G1 REPLICATION -- ENVELOPE, seed 5678
#
# GENERATED by scripts/make_replication_configs.py.  Do not hand-edit: the
# three policies are byte-identical outside `mask.curriculum`, and editing one
# file breaks that guarantee silently.
#
# Ancestor : D:\jepa_phase0\fairvision-glaucoma\checkpoint-ep25\jepa_patch-random_posfix-ep25.pth.tar
#            sha256 e5ad5b0c2aadfa15449409786afbfa39d8b5405b699be8f02f2e540195e97e7b
#            The canonical LOCAL fork point shared by random, oracle/centroid,
#            envelope and cover.  Re-verified before every launch.
# Endpoint : epoch 50 (enforced by campaign_supervisor.py --stop_after_epoch 50;
#            optimization.epochs stays 100 so the cosine schedules are unchanged)
# Seed     : 5678  (meta.seed, read at src/train_patch.py:149; seeds
#            random/numpy/torch/torch.cuda, so crop draws, mask draws, dropout
#            and the DataLoader worker streams all differ between seeds)
data:
  batch_size: 64
  crop_size: 256
  crop_scale:
  - 0.3
  - 1.0
  num_workers: 6
  val_num_workers: 2
  pin_mem: true
  prefetch_factor: 2
  data_dir: D:\jepa_phase0\fairvision-glaucoma\data
  slice_cache_dir: C:\jepa_data\slice_cache
  num_slices: 100
  color_jitter_strength: 0.0
  use_color_distortion: false
  use_gaussian_blur: false
  use_horizontal_flip: false
mask:
  patch_size: 16
  num_enc_masks: 1
  num_pred_masks: 4
  enc_mask_scale:
  - 0.85
  - 1.0
  pred_mask_scale:
  - 0.15
  - 0.2
  aspect_ratio:
  - 0.75
  - 1.5
  allow_overlap: false
  min_keep: 10
  curriculum:
    enabled: true
    mode: mirage_envelope
    T_warm: 25
    T_total: 30
    r_max: 1.0
    ramp_shape: linear
    mirage_guide_dir: D:\jepa_phase0\fairvision-glaucoma\mirage_guides
    mirage_dilate_patches: 0
    mirage_min_block_fill: 0.4
    mirage_min_retina_visible: 0.25
    mirage_max_attempts: 30
    mirage_occupancy_threshold: 0.25
    mirage_spread: true
    mirage_overlap_tolerance: 0.25
meta:
  model_name: vit_base
  pred_depth: 6
  pred_emb_dim: 384
  use_bfloat16: false
  amp_target: false
  load_checkpoint: true
  read_checkpoint: D:\jepa_phase0\fairvision-glaucoma\checkpoint-ep25\jepa_patch-random_posfix-ep25.pth.tar
  seed: 5678
optimization:
  epochs: 100
  lr: 0.00025
  start_lr: 0.0001
  final_lr: 1.0e-06
  warmup: 5
  weight_decay: 0.04
  final_weight_decay: 0.4
  ema:
  - 0.996
  - 1.0
  ipe_scale: 1.0
  accum_steps: 8
  save_every: 5
  patience: 9999
logging:
  folder: D:\jepa_phase0\runs\rep_envelope_s5678
  write_tag: jepa_patch_rep_envelope_s5678
